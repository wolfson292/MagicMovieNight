# Local development override — builds from source instead of pulling the published
# image. Portainer never sees this file.
#
#   docker compose -f compose.yaml -f compose.dev.yaml up -d --build

services:
  app:
    build: .
    image: magicmovienight:dev
